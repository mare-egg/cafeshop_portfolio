<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
        <script src="{{constant('cafeshop\\Bootstrap::APP_URL')}}js/common.js"></script>
        <link href="{{constant('cafeshop\\Bootstrap::APP_URL')}}css/style.css" rel="stylesheet">
        <link href="{{constant('cafeshop\\Bootstrap::APP_URL')}}css/regist.css" rel="stylesheet">
        <title>会員登録</title>
    </head>
    <body>
        {% include 'fade.html.twig' %}
        <input type="hidden" name="entry_url" id="entry_url" value="{{constant('cafeshop\\Bootstrap::ENTRY_URL')}}">
        <form method="post" action="confirm.php">
        <div class="confirm-form">
            <h2>ROA COMFIRM</h2>
            <table>
                <tr>
                    <th>氏名<span class="red">*</span></th>
                    <td>
                        <input type="text" name="family_name" class="text-name" value="{{dataArr.family_name}}" placeholder="例）焙煎">
                        <input type="text" name="first_name" class="text-name" value="{{dataArr.first_name}}" placeholder="例）豆太郎">
                        {% if errArr.family_name | length > 0 %}<br><span class="red">{{ errArr.family_name }}</span>{% endif %}
                        {% if errArr.first_name | length > 0 %}<br><span class="red">{{ errArr.first_name }}</span>{% endif %}
                    </td>
                </tr>
                <tr>
                    <th>氏名かな<span class="red">*</span></th>
                    <td>
                        <input type="text" name="family_name_kana" class="text-name" value="{{dataArr.family_name_kana}}" placeholder="例）ばいせん">
                        <input type="text" name="first_name_kana" class="text-name" value="{{dataArr.first_name_kana}}" placeholder="例）まめたろう">
                        {% if errArr.family_name_kana | length > 0 %}<br><span class="red">{{ errArr.family_name_kana }}</span>{% endif %}
                        {% if errArr.first_name_kana | length > 0 %}<br><span class="red">{{ errArr.first_name_kana }}</span>{% endif %}
                    </td>
                </tr>
                <tr>
                    <th>ユーザー名<span class="red">*</span></th>
                    <td>
                        <input type="text" name="user_name" class="text-name" value="{{dataArr.user_name}}" placeholder="ユーザー名を入力">
                        {% if errArr.user_name | length > 0 %}<br><span class="red">{{ errArr.user_name }}</span>{% endif %}
                    </td>
                </tr>
                <tr>
                    <th>パスワード<span class="red">*</span></th>
                    <td>
                        <input type="password" name="password" class="text-name" value="{{dataArr.password}}" placeholder="パスワードを入力">
                        {% if errArr.password | length > 0 %}<br><span class="red">{{ errArr.password }}</span>{% endif %}
                    </td>
                </tr>
                <tr>
                    <th>性別<span class="red">*</span></th>
                    <td>
                        <div class="means">
                        {# <?php foreach($sexArr as $index => $label) ?> #}
                        {% for index, label in sexArr %}
                           <input type="radio" name="sex" value="{{index}}" id="sex_{{index}}" {% if dataArr.sex == index %} checked="checked" {% endif %}>
                           <label for="sex_{{index}}">{{label}}</label>
                         {% endfor %}
                         {% if errArr.sex | length > 0 %}<br><span class="red">{{errArr.sex}}</span>{% endif %}
                        </div>
                    </td>
                </tr>
                <tr>
                    <th>生年月日<span class="red">*</span></th>
                    <td>
                        <select name="year" >
                            {% for index, label in yearArr %}
                                <option value="{{index}}" {% if dataArr.year == index %} selected {% endif %}>{{ label }}</option>
                            {% endfor %}
                        </select>
                        <select name='month'>
                            {% for index, label in monthArr %}
                                <option value="{{index}}" {% if dataArr.month == index %} selected {% endif %}>{{ label }}</option>
                            {% endfor %}
                        </select>
                        <select name='day'>
                            {% for index, label in dayArr %}
                                <option value="{{index}}" {% if dataArr.day == index %} selected {% endif %}>{{ label }}</option>
                            {% endfor %}
                        </select>
                        {% if errArr.year | length > 0 %}<br><span class="red">{{errArr.year}}</span>{% endif %}
                        {% if errArr.month | length > 0 %}<br><span class="red">{{errArr.month}}</span>{% endif %}
                        {% if errArr.day | length > 0 %}<br><span class="red">{{errArr.day}}</span>{% endif %}
                    </td>
                </tr>
                <tr>
                    <th>郵便番号<span class="red">*</span></th>
                    <td>
                        <input type="text" name="zip1" class="text" value="{{dataArr.zip1}}" id="zip1" size="3" maxlength="3" placeholder="例）000"> - 
                        <input type="text" name="zip2" class="text" value="{{dataArr.zip2}}" id="zip2" size="4" maxlength="4" placeholder="例）0000">
                        <input type="button" name="address_search" value="〒から住所を入力" id="address_search">
                        {% if errArr.zip1 | length > 0 %}<br><span class="red">{{errArr.zip1}}</span>{% endif %}
                        {% if errArr.zip2 | length > 0 %}<br><span class="red">{{errArr.zip2}}</span>{% endif %}
                    </td>
                </tr>
                <tr>
                    <th>住所<span class="red">*</span></th>
                    <td>
                        <input type="text" name="address" class="text-box" value="{{dataArr.address}}" id="address" size="40" placeholder="例）東京都葛飾区南2丁目12番10号">
                        {% if errArr.address | length > 0 %}<br><span class="red">{{errArr.address}}</span>{% endif %}
                    </td>
                </tr>
                <tr>
                    <th>メール<span class="red">*</span></th>
                    <td>
                        <input type="text" name="email" class="text-box" value="{{dataArr.email}}" size="40" placeholder="例）roa.caferia@gmail.com">
                        {% if errArr.email | length > 0 %}<br><span class="red">{{errArr.email}}</span>{% endif %}
                    </td>
                </tr>
                <tr>
                    <th>電話番号<span class="red">*</span></th>
                    <td>
                        <input type="text" name="tel1" class="text-call" value="{{dataArr.tel1}}" size="6" maxlength="6" placeholder="例）000"> -
                        <input type="text" name="tel2" class="text-call" value="{{dataArr.tel2}}" size="6" maxlength="6" placeholder="例）0000"> -
                        <input type="text" name="tel3" class="text-call" value="{{dataArr.tel3}}" size="6" maxlength="6" placeholder="例）0000">
                        {% if errArr.tel1 | length > 0 %}<br><span class="red">{{errArr.tel1}}</span>{% endif %}
                        {% if errArr.tel2 | length > 0 %}<br><span class="red">{{errArr.tel2}}</span>{% endif %}
                        {% if errArr.tel3 | length > 0 %}<br><span class="red">{{errArr.tel3}}</span>{% endif %}
                    </td>
                </tr>
                <tr>
                    <th>サイトを知った<br>きっかけ<span class="red">*</span></th>
                    <td>
                        <div class="means">
                        {# []としてあげることで同じキーでも配列として扱うことができる #}
                        {% for index, label in cueArr %}
                            <input type="checkbox" name="cue[]" value="{{index}}" id="cue_{{index}}" {% if index in dataArr.cue %} checked="checked" {% endif %}>
                            <label for="cue_{{index}}">{{ label }}</label>
                        {% endfor %}
                        {% if errArr.cue | length > 0 %}<br><span class="red">{{errArr.cue}}</span>{% endif %}
                        </div>
                    </td>
                </tr>
                <tr>
                    <th>上記の内容<br>お聞かせ下さい</th>
                    <td>
                        <textarea name="contents" placeholder="例1）〇〇サイトで知りました。〇〇番組で知りましたなど">{{dataArr.contents}}</textarea>
                    </td>
                </tr>
            </table>
            <div>
                <input type="submit" name="confirm" class="post" value="登録確認">
                <a class="back" href="{{constant('cafeshop\\Bootstrap::ENTRY_URL')}}login.php">戻る</a>
                {# <input type="button" name="back" class="postback" value="戻る" onclick="history.back(); return false;"> #}
            </div>
        </div>
        </form>
    </body>
</html>